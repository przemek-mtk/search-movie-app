import styled from "styled-components"

const StyledSearchOptionRadio = styled.div`
   label {
    display: block;
    margin-top: 4px;
  }

  input, span {
    margin: 0;
    padding: 0;
    vertical-align: middle;
    line-height: 14px;
    text-transform: capitalize;
  }

  span {
    padding-left: 4px;
  }

`;

export default StyledSearchOptionRadio;